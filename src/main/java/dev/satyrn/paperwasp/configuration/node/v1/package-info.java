/**
 * Provides the first version of classes for managing configuration nodes in the PaperWasp library.
 * <p>
 * This package includes foundational classes and interfaces for handling various types of configuration nodes
 * within the PaperWasp library. These nodes are designed to encapsulate different data types and structures,
 * providing a consistent and type-safe way to manage configuration data.
 * <p>
 * Future breaking changes will be introduced in subsequent versions of this package (e.g., v2, v3),
 * which will supersede the classes in this package. Users are encouraged to track updates to ensure compatibility
 * with newer versions.
 *
 * @since 3.0.0-paper-api.1.21-R0.1-SNAPSHOT
 */package dev.satyrn.paperwasp.configuration.node.v1;